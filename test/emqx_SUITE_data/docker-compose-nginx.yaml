version: '3.9'
services:
  nginx:
    image: nginx
    container_name: nginx
    ports:
      - 8080:8080
    volumes:
      - ./nginx_default.conf:/etc/nginx/conf.d/default.conf
    networks:
      - emqx_bridge
